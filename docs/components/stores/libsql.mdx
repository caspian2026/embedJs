---
title: LibSQL
---

You can use [LibSQL](https://docs.turso.tech/introduction) a fork of SQLite to store values locally on disk.

## Install LibSQL addon

```bash
npm install @caspian2026/embedjs-libsql
```

## Usage
<CodeGroup>
```ts Partial example
import { RAGApplicationBuilder } from '@caspian2026/embedjs';
import { LibSqlStore } from '@caspian2026/embedjs-libsql';

const app = await new RAGApplicationBuilder()
.setStore(new LibSqlStore({ path: './data.db' }))
```
</CodeGroup>

<Snippet file="missing-store-tip.mdx" />
